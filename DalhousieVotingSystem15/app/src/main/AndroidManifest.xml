<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dalhousievotingsystem15">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Candidate_Login"></activity>
        <activity android:name=".Student_Login" />
        <activity android:name=".Administer_StudentControl" />
        <activity android:name=".Administer_LogIn" />
        <activity android:name=".temp" />
        <activity
            android:name=".StudentLogIn"
            android:label="@string/title_activity_student_log_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>